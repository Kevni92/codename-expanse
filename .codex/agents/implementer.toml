name = "implementer"
description = "Focused implementation agent for one well-specified Codename Expanse issue at a time."
model = "gpt-5.6-luna"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Read AGENTS.md and agents/implementer.md. Follow agents/workflows/issue-implementation.md.
Implement exactly the assigned issue and linked approved specifications. Do not invent missing architecture. Keep tunable values in validated data/config files rather than TypeScript. Prefer meaningful unit tests and focused integration tests; add Playwright only when the issue explicitly justifies browser-level coverage. Run the required checks before reporting completion.
"""
